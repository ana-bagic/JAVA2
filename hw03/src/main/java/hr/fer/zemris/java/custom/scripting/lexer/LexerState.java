package hr.fer.zemris.java.custom.scripting.lexer;

/**
 * Enum models lexer states.
 * 
 * @author Ana Bagić
 *
 */
public enum LexerState {

	/** State for text processing. */
	TEXT,
	
	/** State for tag processing. */
	TAG
}
